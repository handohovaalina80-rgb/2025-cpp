#include<stdio.h>

int main(int argc, char** argv)
{
    std::string str;
    std::cin >> str;
    
    int count[256] = {0};
    
    for (size_t i = 0; i < str.length(); i++) {
        count[static_cast<unsigned char>(str[i])] = count[static_cast<unsigned char>(str[i])] + 1;
    }
    
    for (int i = 0; i < 256; i++) {
        if (count[i] == 2) {
            std::cout << static_cast<char>(i);
            break;
        }
    }
    
    return 0;
}
